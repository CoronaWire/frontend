// Styled components for error text which will be outputted throughout the app when a client-error
// is encountered. Ie. Error text will be displayed when a user unsuccessfully logs into their account


import styled from 'styled-components';

const ErrorText = styled.div`
    color: red;
    font-size: 12px;
`

export default ErrorText;
